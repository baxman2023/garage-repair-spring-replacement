{
  "name": "@copyforge/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsup src/index.ts --format esm --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@copyforge/ai": "workspace:*",
    "@copyforge/core": "workspace:*",
    "@copyforge/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.7"
  }
}
